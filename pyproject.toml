[project]
name = "fdl-estat"
version = "0.1.0"
description = "e-Stat dataset for fdl"
requires-python = ">=3.13"
dependencies = [
    "frozen-ducklake>=0.5.0",
    "dbt-core>=1.10.0",
    "dbt-duckdb>=1.10.0",
    "dlt[ducklake]>=1.17.0",
    "estat-api-dlt-helper>=0.3.0",
    "pyyaml>=6.0",
]
